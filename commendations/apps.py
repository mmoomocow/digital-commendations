from django.apps import AppConfig


class CommendationsConfig(AppConfig):
    """AppConfig for the commendations app."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "commendations"
